- 여기까지 됨: 창이 있는 게임만 실행 중으로 치고, 창이 사라지면 5초 뒤에 끄고, 감시가 중간에 끊겨도 `hdr-owned.flag`를 보고 끈다. 테스트 8가지가 모두 통과했고, 로그온 작업을 다시 시작한 뒤 창 없이 남은 로블록스에는 반응하지 않았다.
- 다음 할 것: 실제 게임 하나(DJMAX 등)를 켜고 끄면서 `watch.log`와 HDR 상태를 확인한다.
- 막힌 것: 모니터 이름 조회(`GET_TARGET_NAME`)가 실패해서 `target <id>`로 나온다. 트레이로 창을 숨기는 게임은 창이 없는 것으로 판정되지만, 지금 목록에는 그런 게임이 없다.
